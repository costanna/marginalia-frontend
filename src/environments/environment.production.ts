export const environment = {
  production: true,
  // Replace with the real Render URL once the API is deployed (phase 8). Angular does not read
  // environment variables at runtime: this value is compiled into the bundle.
  apiUrl: 'https://marginalia-api.onrender.com/api/v1',
};
