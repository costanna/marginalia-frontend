export const environment = {
  production: true,
  // The deployed API on Render. Angular does not read environment variables at run time: this value
  // is compiled into the bundle, so changing the API's address means changing it here and redeploying.
  apiUrl: 'https://marginalia-api-mimv.onrender.com/api/v1',
};
